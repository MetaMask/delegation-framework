# Signer for delegations, quote signing, and EIP-7702 smart account
PRIVATE_KEY=

# EVM RPC for on-chain commands (create, execute, approve) — must match --input-chain
# Arc mainnet example:
ARC_RPC_URL=https://arc-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
BASE_RPC_URL=https://arc-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Optional defaults for `npm run lifi -- quote` (chains; addresses default to PRIVATE_KEY EOA)
# LIFI_QUOTE_INPUT_CHAIN=Arc
# LIFI_QUOTE_OUTPUT_CHAIN=Arc

# Swap route: prefer CLI flags on create (--input-chain, --input-token, --output-chain, --output-token).
# Legacy optional defaults (only used when create omits route flags):
# LIFI_FROM_TOKEN=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
# LIFI_TO_TOKEN=0x4200000000000000000000000000000000000006
# LIFI_TO_CHAIN=8453

# Default execute input amount (token atoms, e.g. 1000000 = 1 USDC)
LIFI_FROM_AMOUNT=1000000

# Li.FI API slippage (decimal, e.g. 0.005 = 0.5%)
LIFI_SLIPPAGE=0.005

# Optional Li.FI Partner Portal API key (for `npm run lifi` discovery commands)
# LIFI_API_KEY=

# Default periodic budget for `delegation create`
LIFI_PERIOD_AMOUNT=10000000
LIFI_PERIOD_DURATION=86400

# On-chain slippage cap in enforcer terms (basis points)
LIFI_SLIPPAGE_BPS=50

# Optional: override LiFi diamond on source chain at create (default from Li.FI chains API)
# LIFI_DIAMOND=0x1231DEB6f5749EF6cE6943a275A1D3E7486F4EaE

# 1Shot public relayer (optional override)
RELAYER_URL=https://relayer.1shotapi.com/relayers

# Chainlink price gate (create-chainlink only; rule params are CLI flags)
CHAINLINK_PRICE_FEED=0x71041dddad3595F9CEd3DcCFBe3D1F4b0a16Bb70
CHAINLINK_MAX_STALE_SECONDS=120
CHAINLINK_MIN_GAP_SECONDS=300

# --- Cross-chain: Base USDC → Solana (comment out same-chain LIFI_TO_* above first)
# afterHook does not verify destination delivery on non-EVM chains.
# Verify chain IDs: GET https://li.quest/v1/chains?chainTypes=SVM,UTXO
# LIFI_TO_CHAIN=1151111081099710
# LIFI_TO_TOKEN=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
# Native SOL LiFi id: 11111111111111111111111111111111
# LIFI_OUTPUT_RECIPIENT=YOUR_SOLANA_PUBKEY
# Optional bytes32 overrides from LiFi quote tooling (if auto-encoding differs):
# LIFI_OUTPUT_ASSET_ID=0x...
# LIFI_OUTPUT_RECIPIENT_BYTES32=0x...

# --- Cross-chain: Base USDC → Bitcoin
# LIFI_TO_CHAIN=20000000000001
# LIFI_TO_TOKEN=bitcoin
# LIFI_OUTPUT_RECIPIENT=bc1q...
# LIFI_OUTPUT_ASSET_ID=0x...
# LIFI_OUTPUT_RECIPIENT_BYTES32=0x...
